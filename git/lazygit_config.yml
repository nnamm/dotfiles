customCommands:
  - key: "C"
    context: "files"
    command: "czg"
    output: terminal
# gui:
#   nerdFontsVersion: "3"
git:
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never
